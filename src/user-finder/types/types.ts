export type UserProperties = {
  name: string;
  location: string;
  bronze_badge_count: number;
  account_id: number;
  reputation: number;
  link: string;
};

export type Country = {
  name: string;
};
